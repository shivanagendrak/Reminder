import { View, Text } from "react-native";

export default function MedicationScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ fontSize: 24 }}>Medication Screen</Text>
    </View>
  );
}
